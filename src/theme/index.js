import createTheme from '@mui/material/styles/createTheme'

export default createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#cccccc'
    },
    secondary: {
      main: '#000000'
    }
  },
})